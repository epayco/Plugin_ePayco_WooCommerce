window.addEventListener("load", (function () {
    var e;
    !function () {
        const e = document.querySelector("table.form-table");
        return e && e.hasChildNodes()
    }() ? function () {
        const e = document.querySelector("table.form-table");
        e?.previousElementSibling.remove(), e?.previousElementSibling.remove(), e?.nextElementSibling.remove()
    }() : (function () {
        if (document.querySelector(".wc-admin-breadcrumb").style.display = "none", document.querySelector(".ep-header-logo")) document.querySelector(".ep-header-logo").style.display = "none"; else {
            const e = document.querySelectorAll("#mainform > p:not(.submit)");
            e[0] && (e[0].style.display = "none")
        }
        const e = document.querySelector("#mainform > h2");
        0 === e.classList.length && (e.style.display = "none"), document.querySelectorAll(".ep-hidden-field-description").forEach((e => {
            e.closest("tr").style.display = "none"
        }))
    }(), function () {
        const e = document.querySelectorAll(".limit-title-max-length");
        e && e.forEach((e => {
            e.setAttribute("maxlength", "85")
        }))
    }(), function () {
        const e = document.querySelectorAll("th.titledesc");
        for (let o = 0; o < e.length; o++) e[o].classList.add("ep-field-text-title"), e[o] && e[o].children[0] && e[o].children[0].children[0] && e[o].children[0].children[0].classList.add("ep-field-text-subtitle")
    }(), (e = window.jQuery)(".ep-child").change((function () {
        const o = e(this).data("group"), t = e('.ep-selectall[data-group="' + o + '"]'),
            n = e('.ep-child[data-group="' + o + '"]:checked').length > 0;
        t.prop("checked", n)
    })).change(), window.onbeforeunload = "", e("input, textarea, select, checkbox").on("change", (function () {
        window.onbeforeunload = function (e) {
            return e.preventDefault(), !0
        }
    })), e(".ep-selectall").click((function () {
        const o = e(this).data("group");
        e('.ep-child[data-group="' + o + '"]').prop("checked", this.checked).change()
    })))
}));